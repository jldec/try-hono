{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "pre-ship": "wrangler deploy --dry-run --outdir dist --minify src/index.ts",
    "ship": "wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "hono": "^4.2.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "wrangler": "^3.47.0"
  }
}
